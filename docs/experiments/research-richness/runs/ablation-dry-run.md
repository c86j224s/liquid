# Ablation Dry Run

- Status: blocked: runtime unavailable
- Earliest observable blocker: no live research runtime was exercised locally, and the active research path does not expose a configurable context/window knob in `src/ai_runtime.rs`
- Recommendation: keep default context/window unchanged until a configurable knob and live benchmark run exist
